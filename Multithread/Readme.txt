Hi all, This is a multithreaded program where i had shown that two threads will be created which will corrupt the data,as synchronization is not acheived.

To execute the program:
1) Generate thread executable(make thread)
2) Execute ./thread

